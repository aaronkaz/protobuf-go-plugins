version: v1
managed:
  enabled: true
  go_package_prefix:
    default: examples
plugins:
  - plugin: sam-go
    out: examples
    opt: paths=source_relative
  - plugin: enum-desc-go
    out: examples
    opt: paths=source_relative

